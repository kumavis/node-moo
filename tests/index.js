var test = require('tape')
var Castle = require('../lib/castle.js')

test('can create a Castle', function (t) {
  var castle = Castle.create()
  t.ok(true)
})

test('can create a Castle', function (t) {
  var castle = Castle.create()
  t.ok(true)
})